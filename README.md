# agent-sec-scanners

Monorepo for the `@miaggy` scanner family: one shared audit engine, provider-specific packs.

| Package | npm | Status |
|---|---|---|
| `packages/core` | `@miaggy/core` | skeleton — engine extraction in progress |

`bedrock-security-mcp` continues to ship from [its existing repo](https://github.com/AUAggy/bedrock-security-mcp) until the extraction completes.
